import * as React from "react";

class Header extends React.Component {
    render() {
        return (
            <div>
                this is Header
            </div>
        );
    }
}

export default Header;